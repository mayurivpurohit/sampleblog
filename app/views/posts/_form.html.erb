  <%= simple_form_for @post do |f|%>
  <%= f.hidden_field :user_id ,value: "#{current_user.id}" %>
   <%= f.input :title %>
   <%= f.input :content, :as => :text, input_html: { class: 'special' } %>
     <br/><%= f.button :submit, :class => 'btn btn-primary' %>
  <% end %>
