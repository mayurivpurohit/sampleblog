  <%= form_for @post do |f|%>
    <strong><%= f.label :title %></strong><%= f.text_field :title %>
    <strong><%= f.label :content %></strong><%= f.text_area:content, :rows => 10, :cols => 5 %>
     <br/><%= f.submit "Submit", :class => 'btn btn-primary' %>
  <% end %>
